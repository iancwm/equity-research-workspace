# 2.0.0 Release Verification

This file records release-gate evidence without storing credentials, proprietary research, or model transcripts.

## Completed locally

- Offline unit and package-contract tests pass on Linux with Python 3.12.
- The canonical skill archive builds reproducibly with one safe top-level directory and no symlinks.
- Claude Code marketplace metadata passes `claude plugin validate`.
- The canonical initialized and migrated synthetic workspaces pass structural validation.

## Required before tagging

- Confirm the GitHub Actions matrix on Python 3.9, 3.12, and 3.13 across Linux, macOS, and Windows.
- Install the built archive in Claude Code and Codex and run one non-sensitive smoke task on each.
- Upload the archive once through both the Claude and OpenAI hosted Skills APIs.

The authenticated installation and hosted-upload checks were not run during repository implementation because they change external state and require the maintainer's accounts. Live Claude and Codex behavioral evals were also skipped because they are opt-in and cost-bearing. Record host, model, date, capabilities, and sanitized results here or in the release PR before creating tag `v2.0.0`.
